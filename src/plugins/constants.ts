/**
 * Plugin layer constants (Phase 04).
 */

/** Prefix for plugin tools when exposed to the Hub (e.g. plugin_hello_tool_name). */
export const PLUGIN_TOOL_PREFIX = "plugin_";

/** Prefix for plugin prompts (e.g. plugin_hello_prompt_name). */
export const PLUGIN_PROMPT_PREFIX = "plugin_";

/** URI scheme for plugin resources (e.g. plugin://hello/resource_name). */
export const PLUGIN_RESOURCE_URI_SCHEME = "plugin";

/** Prefix for plugin skills when exposed (e.g. skill_plugin_hello_skill_name). */
export const PLUGIN_SKILL_PREFIX = "skill_plugin_";
