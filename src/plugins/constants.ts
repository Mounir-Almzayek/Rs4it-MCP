/**
 * Plugin layer constants (Phase 04).
 */

/** Prefix for plugin tools when exposed to the Hub (e.g. plugin:hello:tool_name). */
export const PLUGIN_TOOL_PREFIX = "plugin:";

/** Prefix for plugin prompts (e.g. plugin:hello:prompt_name). */
export const PLUGIN_PROMPT_PREFIX = "plugin:";

/** URI scheme for plugin resources (e.g. plugin://hello/resource_name). */
export const PLUGIN_RESOURCE_URI_SCHEME = "plugin";

/** Prefix for plugin skills when exposed (e.g. skill:plugin:hello:skill_name). */
export const PLUGIN_SKILL_PREFIX = "skill:plugin:";
