/**
 * Plugin layer constants (Phase 04).
 */

/** Prefix for plugin tools when exposed to the Hub (e.g. plugin_<id>_<tool_name>). */
export const PLUGIN_TOOL_PREFIX = "plugin_";

/** URI scheme for plugin resources (e.g. plugin://<id>/<resource_uri>). */
export const PLUGIN_RESOURCE_URI_SCHEME = "plugin";
