# Required for admin authentication (Phase 10). Min 16 characters.
SESSION_SECRET=change-me-to-a-long-random-secret

# Optional: path to admin credentials file (default: ../config/admin-credentials.json)
# ADMIN_CREDENTIALS_PATH=/path/to/admin-credentials.json
